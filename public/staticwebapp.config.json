{
  "routes": [
    { "route": "/legal/*", "allowedRoles": ["anonymous", "authenticated"] }
  ],
  "navigationFallback": {
    "rewrite": "/index.html",
    "exclude": ["/assets/*", "/*.{js,css,png,ico,json}"]
  },
  "backend": {
    "uri": "https://fn-strata-reports.azurewebsites.net"
  }
}
